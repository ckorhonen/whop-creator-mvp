name: Regenerate Complete Lockfile

on:
  workflow_dispatch:
    inputs:
      force:
        description: 'Force regeneration even if lockfile exists'
        required: false
        default: 'true'
  push:
    branches:
      - automated-lockfile-fix
    paths:
      - '.github/workflows/regenerate-lockfile.yml'

permissions:
  contents: write
  pull-requests: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    name: Regenerate package-lock.json
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          echo "✅ Git configured"
      
      - name: Remove incomplete lockfile
        run: |
          echo "=== Removing incomplete lockfile ==="
          if [ -f package-lock.json ]; then
            echo "Found existing package-lock.json"
            
            # Check for placeholder hashes
            if grep -q "example-integrity-hash\|example-.*-hash" package-lock.json; then
              echo "⚠️  Found placeholder integrity hashes - lockfile is invalid"
              PLACEHOLDER_COUNT=$(grep -c "example-.*-hash" package-lock.json || echo "0")
              echo "Found $PLACEHOLDER_COUNT placeholder hashes"
            fi
            
            rm package-lock.json
            echo "✅ Removed incomplete lockfile"
          else
            echo "No existing lockfile found"
          fi
      
      - name: Clean npm cache
        run: |
          echo "=== Cleaning npm cache ==="
          npm cache clean --force
          echo "✅ Cache cleaned"
      
      - name: Generate complete package-lock.json
        run: |
          echo "=== Generating Complete Lockfile ===="
          echo "This will download and verify all dependencies..."
          
          # Use npm install (not --package-lock-only) to ensure proper integrity hash generation
          # Then we'll remove node_modules to keep the commit clean
          npm install
          
          if [ -f package-lock.json ]; then
            LOCKFILE_LINES=$(wc -l < package-lock.json)
            echo "✅ Generated package-lock.json with $LOCKFILE_LINES lines"
            
            # Verify it has proper integrity hashes (not placeholders)
            if grep -q "example-integrity-hash\|example-.*-hash" package-lock.json; then
              echo "❌ ERROR: Generated lockfile still contains placeholder hashes!"
              echo "This should not happen. Showing problematic entries:"
              grep -n "example-.*-hash" package-lock.json | head -10
              exit 1
            fi
            
            # Verify it has real integrity hashes
            if grep -q '"integrity": "sha' package-lock.json; then
              INTEGRITY_COUNT=$(grep -c '"integrity": "sha' package-lock.json || echo "0")
              echo "✅ Lockfile contains $INTEGRITY_COUNT proper integrity hashes"
            else
              echo "⚠️  Warning: No integrity hashes found"
            fi
          else
            echo "❌ Failed to generate lockfile"
            exit 1
          fi
      
      - name: Remove node_modules (keep only lockfile)
        run: |
          echo "=== Cleaning up node_modules ==="
          if [ -d node_modules ]; then
            rm -rf node_modules
            echo "✅ Removed node_modules (keeping only lockfile)"
          fi
      
      - name: Verify lockfile
        run: |
          echo "=== Verifying Lockfile ==="
          
          # Verify lockfile structure
          if npm ls --package-lock-only 2>&1 | grep -q 'npm ERR!'; then
            echo "⚠️  Warning: Some dependency issues detected"
            npm ls --package-lock-only || true
          else
            echo "✅ Lockfile is valid"
          fi
          
          echo ""
          echo "=== Lockfile Statistics ==="
          echo "Total lines: $(wc -l < package-lock.json)"
          echo "Total packages: $(grep -c '"resolved":' package-lock.json || echo '0')"
          echo "Total integrity hashes: $(grep -c '"integrity": "sha' package-lock.json || echo '0')"
          echo "File size: $(du -h package-lock.json | cut -f1)"
      
      - name: Commit and push changes
        run: |
          echo "=== Committing Changes ==="
          
          git add package-lock.json
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          
          git commit -m "🔧 Regenerate complete package-lock.json with valid integrity hashes

This fixes the incomplete lockfile that was causing deployment issues.

Changes:
- Complete dependency resolution with all transitive dependencies
- Proper SHA-512 integrity hashes for all packages (no placeholders)
- Verified lockfile format

Packages fixed:
- @whop-sdk/core
- wrangler
- eslint
- @babel/* packages
- All other dev dependencies

Fixes workflow failures and enables proper dependency caching."
          
          echo "=== Pushing Changes ==="
          echo "Pushing to branch: ${{ github.ref_name }}"
          git push
          
          echo "✅ Changes committed and pushed to ${{ github.ref_name }}"
